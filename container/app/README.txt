Requirements:
python3, pip3, linux host
Install:
pip3 install -r requirements.txt
Configuring:
1. make use of the templates folder (edit the templates within)
2. etc (check the main.conf file in the config folder as well), add extra log generators inside


TOKENS:

%%PUB_IPV4%%
%%IPV6%%
%%PRIV_IPV4%%
%%HOSTNAME%%
%%URI%%
%%USER%%
%%EMAIL%%
%%TS_1%%